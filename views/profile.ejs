<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Your Profile</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-zinc-900 text-white min-h-screen flex flex-col">

  <!-- Navbar -->
  <header class="w-full px-8 py-4 flex justify-between items-center bg-zinc-800 shadow">
    <h1 class="text-2xl font-semibold text-orange-400">Chess Game</h1>
    <nav class="flex gap-6">
      <a href="/" class="hover:text-orange-400 transition">Home</a>
      <a href="/logout" class="hover:text-orange-400 transition">Logout</a>
    </nav>
  </header>

  <!-- Profile Info -->
  <main class="flex-grow flex items-center justify-center">
    <div class="bg-zinc-800 p-8 rounded-lg shadow-md w-[320px]">
      <h2 class="text-2xl font-semibold mb-4 border-b border-zinc-700 pb-2">Your Profile</h2>
      <div class="mb-4">
        <p class="text-sm text-gray-400">Full Name</p>
        <p class="text-lg"><%= user.fullname %></p>
      </div>
      <div class="mb-4">
        <p class="text-sm text-gray-400">Email</p>
        <p class="text-lg"><%= user.email %></p>
      </div>
    </div>
  </main>

</body>
</html>
